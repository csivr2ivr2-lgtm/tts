{
  "name": "aharon-tts-server",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/audio.js && node --check src/voice-profile.js && node --check src/stt.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pocket-tts-onnx": "^0.1.0",
    "onnxruntime-node": "^1.29.0",
    "ogg-opus-decoder": "^1.7.5",
    "@huggingface/transformers": "^3.8.1"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
