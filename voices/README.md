# Private voice input

Place the source recording here as `ari.wav` only when rebuilding the voice profile. Personal voice recordings are intentionally ignored by Git and are not stored in this repository.
